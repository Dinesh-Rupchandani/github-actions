name: container

on: push

jobs:

  first:
    runs-on: ubuntu-latest
    container:
      image: node:16-alpine3.11
    steps:
      - name: log node version
        run: |
          node -v
          ls -a   